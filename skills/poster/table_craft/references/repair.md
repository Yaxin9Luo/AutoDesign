# Table repair

Flag table-looking prose, native replacements for available source evidence, duplicate full reconstructions, unreadable rows/columns, missing units or context, and style drift. Flag orphan metrics with no baseline, dataset, protocol, denominator, or direction.

Repair information design first: choose the job, reduce rows/columns, rewrite headers, split wide comparisons, and add the readout. Then repair CSS toward restrained rules and one emphasis treatment. Fill remaining panel capacity with table-adjacent definitions or caveats, never invented values or generic filler.
