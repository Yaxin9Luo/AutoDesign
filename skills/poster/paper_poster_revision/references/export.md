# Poster export

Re-render at the intended viewport or page size after edits. Check page scroll dimensions, bounds, broken images, overflow, chart labels, header alignment, and bottom-row density. Confirm each source figure/table is the intended paper item, readable, and free of clipped axes, legends, captions, or body text.

For a standalone HTML copy, keep the normal editable HTML and inline its local image assets as `data:` URIs. Verify no `layers/...` references remain, the file opens directly, and its screenshot matches the ordinary export. Close manual browser servers and sessions when done.
