# Layout repair

Render and inspect the full poster before patching. Verify every column and section against paper evidence, then classify the issue as content priority, layout, asset correctness, or typography. Measure figure bottom, last-text bottom, section bottom, and page overflow when repairing empty space.

Use the paper's framework figure in Method and its headline benchmark/table in Evaluation. Analysis should use scaling, ablation, diagnostics, or limitations evidence, not generic future work. Fill local slack with concise paper-grounded readouts, compact rows, or callouts inside the same source-flow unit before resizing assets. Repair all columns, not only the named section.

Patch existing scoped CSS and DOM structure. Fix chart labels visually. Preserve typography and use the actual user-supplied source assets. Do not make broad palette, structure, or content changes for a local revision.
