# Reference Style Extraction

Plan-only guidance for the external Reference Style Agent. It extracts transferable style and geometry, never target content.

## Stage: plan

Inspect at target size, map macro body regions before styling, measure them, then separate chrome from content. Read `output_contract_v4` first, `region_geometry` while mapping, `chrome_rules` while normalizing, and `failure_checks` before rendering and approval. Preserve style only; never copy reference text or assets.
